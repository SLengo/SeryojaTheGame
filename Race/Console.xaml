<Window x:Class="Race.Console"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Race"
        mc:Ignorable="d" WindowStyle="ToolWindow"
        Title="Console" Height="300" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <ScrollViewer Name="ScrollMainOutput">
            <TextBlock Name="MainOutput" Background="Black" Foreground="White"/>
        </ScrollViewer>
        <Border Grid.Row="1" BorderThickness="2" BorderBrush="#cccdce">
        <TextBox Name="MainInput" Background="Black" Foreground="White" KeyDown="MainInput_KeyDown" />
        </Border>
    </Grid>
</Window>
